name: Release

on:
  push:
    tags:
      - "*"

permissions:
  contents: write
  packages: write

env:
  APP_NAME: embyproxy
  REGISTRY: ghcr.io

jobs:
  build-binaries:
    name: Build ${{ matrix.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: windows-x64
            goos: windows
            goarch: amd64
            extension: .exe
            archive: zip
          - name: windows-x86
            goos: windows
            goarch: "386"
            extension: .exe
            archive: zip
          - name: linux-x64
            goos: linux
            goarch: amd64
            extension: ""
            archive: tar.gz
          - name: linux-x86
            goos: linux
            goarch: "386"
            extension: ""
            archive: tar.gz

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Build binary
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          mkdir -p dist
          binary="${APP_NAME}-${{ matrix.name }}${{ matrix.extension }}"
          version="${GITHUB_REF_NAME:-dev}"
          commit="${GITHUB_SHA::7}"
          built_at="$(TZ=Asia/Shanghai date +'%Y-%m-%dT%H:%M:%S%:z')"
          ldflags="-s -w"
          ldflags="${ldflags} -X embyproxy/internal/buildinfo.Version=${version}"
          ldflags="${ldflags} -X embyproxy/internal/buildinfo.Commit=${commit}"
          ldflags="${ldflags} -X embyproxy/internal/buildinfo.BuiltAt=${built_at}"
          go build -trimpath -ldflags="${ldflags}" -o "dist/${binary}" ./cmd/embyproxy
          cp .env.example dist/.env.example

          if [ "${{ matrix.archive }}" = "zip" ]; then
            (cd dist && zip "${APP_NAME}-${{ matrix.name }}.zip" "${binary}" .env.example)
          else
            (cd dist && tar -czf "${APP_NAME}-${{ matrix.name }}.tar.gz" "${binary}" .env.example)
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.APP_NAME }}-${{ matrix.name }}
          path: |
            dist/*.zip
            dist/*.tar.gz
          if-no-files-found: error

  publish-release:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: build-binaries
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          pattern: ${{ env.APP_NAME }}-*
          merge-multiple: true

      - name: Create checksums
        shell: bash
        run: |
          cd dist
          sha256sum * > sha256sums.txt

      - name: Publish release assets
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG_NAME: ${{ github.ref_name }}
        run: |
          if gh release view "${TAG_NAME}" >/dev/null 2>&1; then
            gh release delete-asset "${TAG_NAME}" SHA256SUMS.txt -y || true
            gh release delete-asset "${TAG_NAME}" sha256sums.txt -y || true
            gh release upload "${TAG_NAME}" dist/* --clobber
          else
            gh release create "${TAG_NAME}" dist/* --title "${TAG_NAME}" --generate-notes
          fi

  publish-docker:
    name: Publish Docker image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Normalize image name
        id: image
        shell: bash
        run: echo "name=${GITHUB_REPOSITORY_OWNER,,}/embyproxy" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ steps.image.outputs.name }}
          tags: |
            type=ref,event=tag
            type=raw,value=latest

      - name: Build metadata
        id: build_meta
        shell: bash
        run: |
          echo "version=${GITHUB_REF_NAME:-dev}" >> "$GITHUB_OUTPUT"
          echo "commit=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
          echo "built_at=$(TZ=Asia/Shanghai date +'%Y-%m-%dT%H:%M:%S%:z')" >> "$GITHUB_OUTPUT"

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: true
          provenance: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ steps.build_meta.outputs.version }}
            COMMIT=${{ steps.build_meta.outputs.commit }}
            BUILT_AT=${{ steps.build_meta.outputs.built_at }}
